# 🎮 ECard — Kaiji Web Game

ECard is a web-based version of the card game featured in the anime *Kaiji*.  
This project is built using **Python (Flask)** and provides a simple web interface where players can enter their name, start the game, and read the rules.

---

## 🧰 Requirements

Make sure you have the following installed on your system:

- **Python 3.10+**
- **pip** (Python package manager)
- **virtualenv** (optional, but recommended)
- **Git** (if you’re cloning from GitHub)

---

## ⚙️ Setup Instructions

### 1. Clone the Repository
```bash
git clone https://github.com/Giovannip11/ECard.git
cd ECard
```
### Create Virtual Environment
```bash
python -m venv .venv
```
### Activate Virtual Environment on linux/macOS
```
source .venv/bin/activate
```

### Activate Virtual Environment on Windows
```
.venv\Scripts\activate
```
### Install Flask
```
pip install flask
```
### Run the aplication
```
export FLASK_APP=app
flask run
```
